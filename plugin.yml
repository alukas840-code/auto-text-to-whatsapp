name: DaurenBot
version: 1.0.0
main: com.dauren.DaurenBotPlugin
api-version: "1.20"
description: |
  DaurenBot plugin acts as a hub between WhatsApp and an AI backend.  It
  implements task scheduling and reminder logic and suppresses console
  output.  Actual integration with WhatsApp and the AI must be
  provided by a separate bridge implementation.
author: YourName